# NOTE: re-read the inputrc file with C-x C-r.
$include /etc/inputrc

# Display a list of the matching files
set show-all-if-ambiguous on

# Perform partial (common) completion on the first Tab press, only start cycling full results on the second Tab press (from bash version 5)
set menu-complete-display-prefix on
set colored-stats on
set completion-ignore-case on
set completion-map-case on
set bell-style none

# Open file explorer
"\C-o": "\C-ucdi\C-j"

# Open man page for the what ever typed in the terminal
"\M-h": "\C-aman \C-m"

# Kill whole line
"\e\e": kill-whole-line
"\el": kill-whole-line

# If there are multiple matches for completion, Tab and Shift-Tab should cycle through them all
"\t": menu-complete
"\e[Z": menu-complete-backward

# Cycle through history based on characters already typed on the line
"\e[A": history-search-backward
"\e[B": history-search-forward
"\C-p": history-search-backward
"\C-n": history-search-forward

# Keep Ctrl-Left and Ctrl-Right working when the above are used
"\e[1;5C": forward-word
"\e[1;5D": backward-word


$if Bash

  # edit the path
  "\C-xp": "PATH=${PATH}\e\C-e\C-a\ef\C-f"

  # insert a backslash (testing backslash escapes in sequences and macros)
  "\C-x\\": "\\"

  # Quote the current or previous word
  "\C-xq": "\eb\"\ef\""

  # Add a binding to refresh the line, which is unbound
  "\C-xr": redraw-current-line

  # Edit variable on current line.
  "\M-\C-v": "\C-a\C-k$\C-y\M-\C-e\C-a\C-y="
$endif


# [Escape code references] (https://en.wikipedia.org/wiki/ANSI_escape_code)
# The following GNU Emacs style escape sequences are available when specifying key sequences:
# \C-         control prefix (Ctrl)
# \M-         meta prefix (Alt)
# \e          escape (Esc), simulate Alt key
# \\          backslash (\)
# \"          a double quotation mark (")
# \'          a single quote or apostrophe (')
#
# \e[A        Up
# \e[B        Down
# \e[C        Right
# \e[D        Left
# \e[F        End
# \e[G        Keypad 5
# \e[H        Home
# \e[3~       Delete
# \e[1P       F1
# \e[1Q       F2
# \e[1R       F3
# \e[1S       F4
