# Select theme, run `bat --list-themes` for a list of theme
#--theme="TwoDark"

# Enable italic text on the terminal
#--italic-text=always

# Disable automatic paging
#--paging=never

# Enable mouse scrolling in `bat` when running inside tmux.
# This might disable text selection, unless you press shift.
#--pager="less --RAW-CONTROL-CHARS --quit-if-one-screen --mouse"

# Syntax mappings: map a certain filename pattern to a language.
--map-syntax "*.ino:C++"
--map-syntax='*.conf:INI'
--map-syntax ".ignore:Git Ignore"

# Show line numbers, Git modifications and file header (but no grid)
--style="plain"

