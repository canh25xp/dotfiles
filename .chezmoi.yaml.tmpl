format : yaml

progress : true

edit:
  apply : true
  watch : true

git:
  autoCommit : true
  autoPush : false

{{ if eq .chezmoi.os "windows" -}}
diff:
  pager: cat
{{ else }}
diff:
  command: "delta"
  pager : "delta"
{{- end }}

merge:
  command: nvim
  args:
    - -d
    - "{{ "{{" }} .Destination {{ "}}" }}"
    - "{{ "{{" }} .Source {{ "}}" }}"
    - "{{ "{{" }} .Target {{ "}}" }}"

data:
{{- if and (eq .chezmoi.os "linux") (.chezmoi.kernel.osrelease | lower | contains "microsoft") }}
  wsl : true
{{- else }}
  wsl : false
{{- end}}
  fullName : "Ngô Văn Cảnh"
  nickName : "Michael"
  email : "canh25xp@gmail.com"
  knoxEmail : "vancanh.ng@samsung.com"
  knoxID : "vancanh.ng"
  knoxPW : "********"
  p4Port : "107.113.53.156{{ ":" }}1716"
  p4Client : "SRV_WIFI_PC_VAN_CANH_NGO_1716"
  defaultShell: "pwsh"

{{- if eq .chezmoi.os "windows" }}
cd:
  command: "pwsh"
{{- end }}
